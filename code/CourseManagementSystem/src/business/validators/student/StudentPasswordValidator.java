package business.validators.student;

import business.validators.Validator;
import persistence.domain_model.Student;

public class StudentPasswordValidator implements Validator<Student> {

	@Override
	public boolean validate(Student object) {
		// TODO Auto-generated method stub
		return true;
	}
	
}
